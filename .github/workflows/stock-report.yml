name: Daily Stock Report

on:
  schedule:
    # 국장: UTC 06:30 트리거 → 딜레이 감안 KST 16시쯤 도착
    - cron: '30 6 * * 1-5'
    # 미장: UTC 20:00 트리거 → 딜레이 감안 ET 16:30쯤 도착
    - cron: '0 20 * * 1-5'
  workflow_dispatch:
    inputs:
      market:
        description: '시장 선택'
        required: false
        default: 'ALL'
        type: choice
        options:
          - US
          - KR
          - ALL

permissions:
  contents: write

jobs:
  generate-report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set date and market
        run: |
          echo "TODAY=$(date +%Y%m%d)" >> $GITHUB_ENV
          HOUR=$(date -u +%H)
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "MARKET=${{ inputs.market }}" >> $GITHUB_ENV
          elif [ "$HOUR" -lt 12 ]; then
            echo "MARKET=KR" >> $GITHUB_ENV
          else
            echo "MARKET=US" >> $GITHUB_ENV
          fi

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install yfinance pandas matplotlib numpy

      - name: Generate report
        run: |
          mkdir -p reports
          python3 stock_report.py --market ${{ env.MARKET }} \
            --output reports/report_${{ env.MARKET }}_${{ env.TODAY }}.txt

      - name: Collect attachments
        run: |
          FILES="reports/report_${{ env.MARKET }}_${{ env.TODAY }}.txt"
          for f in reports/risk_chart_*.png; do
            [ -f "$f" ] && FILES="$FILES,$f"
          done
          echo "ATTACHMENTS=$FILES" >> $GITHUB_ENV

      - name: Commit and push report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add reports/
          git diff --staged --quiet || (git commit -m "report: $(date +%Y-%m-%d) ${{ env.MARKET }} 투자 보고서" && git push)

      - name: Set email subject
        run: |
          if [ "${{ env.MARKET }}" = "KR" ]; then
            echo "SUBJECT=[국장] 52주 신고가 보고서 - ${{ env.TODAY }}" >> $GITHUB_ENV
          elif [ "${{ env.MARKET }}" = "US" ]; then
            echo "SUBJECT=[미장] 52주 신고가 보고서 - ${{ env.TODAY }}" >> $GITHUB_ENV
          else
            echo "SUBJECT=[투자] 52주 신고가 보고서 - ${{ env.TODAY }}" >> $GITHUB_ENV
          fi

      - name: Send email notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.GMAIL_ADDRESS }}
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: ${{ env.SUBJECT }}
          to: pooq1553@gmail.com
          from: 투자보고서봇
          body: |
            ${{ env.MARKET }} 시장 장마감 후 52주 신고가 보고서입니다.
          attachments: ${{ env.ATTACHMENTS }}
